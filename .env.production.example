# Copy to .env.production for local production builds (npm run build).
# GitHub Actions sets these in deploy-pages.yml (push main) and deploy-gh-pages-branch.yml (manual).

VITE_BASE_PATH=/Nymo/
VITE_API_BASE_URL=https://chat-app-anzi.onrender.com
